{
  "name": "docx-pdf-converter",
  "version": "1.0.0",
  "description": "A tool to modify DOCX files and convert them to PDF",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "fix-permissions": "chmod +x ./squashfs-root/AppRun && echo 'Permissions fixed for AppRun.'",
    "prepare": "npm run fix-permissions && echo 'Project prepared for execution.'"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "unzipper": "^0.12.3",
    "xmldom": "^0.6.0"
  }
}
